apiVersion: v1
kind: Secret
metadata:
  name: apipulse-secrets
  namespace: apipulse
type: Opaque
# Base64-encode your actual values: echo -n "value" | base64
# These are EXAMPLE values — replace before deploying!
stringData:
  DB_USER: "apipulse"
  DB_PASSWORD: "REPLACE_WITH_STRONG_PASSWORD"
  SECRET_KEY: "REPLACE_WITH_64_CHAR_RANDOM_STRING"
  REDIS_PASSWORD: "REPLACE_WITH_REDIS_PASSWORD"
